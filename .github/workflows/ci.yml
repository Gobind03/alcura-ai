name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  lint-python:
    name: Python Lint (ruff)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"

      - name: Install ruff
        run: pip install ruff

      - name: Check formatting
        run: ruff format --check alcura_ai/

      - name: Lint
        run: ruff check alcura_ai/

  lint-js:
    name: JavaScript Lint (eslint)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install dependencies
        run: yarn install --frozen-lockfile || yarn install

      - name: Lint
        run: yarn lint

  # Uncomment once you have a Frappe bench CI environment configured.
  # tests:
  #   name: Frappe Tests
  #   runs-on: ubuntu-latest
  #   needs: [lint-python, lint-js]
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Run tests
  #       run: bench run-tests --app alcura_ai
